import { ResetPasswordPage } from "./components/recover-pw";
import { SignContainer } from "@/components/sign-container";

export const RecoverPage = () => {
  return (
    <SignContainer>
      <ResetPasswordPage />
    </SignContainer>
  );
};
